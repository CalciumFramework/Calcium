pool:
  vmImage: 'vs2017-win2016'

variables:
  buildConfiguration: 'Release'

steps:
  - script: dotnet restore Source/Framework/Codon.sln
  - script: dotnet build Source/Framework/Codon.sln --configuration $(buildConfiguration)