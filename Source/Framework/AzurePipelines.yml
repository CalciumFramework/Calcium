pool:
  vmImage: 'vs2017-win2016'

variables:
  buildConfiguration: 'Release'

steps:
  - script: echo DV Starting Restore
  - script: dotnet restore
  - script: echo DV Restore Complete
  - script: dotnet build --configuration $(buildConfiguration)