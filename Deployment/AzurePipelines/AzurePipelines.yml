pool:
  vmImage: 'vs2017-win2016'

variables:
  buildConfiguration: 'Release'

steps:
  - script: dotnet restore
  - script: dotnet build \Framework\Source\Framework\Codon.sln --configuration $(buildConfiguration)